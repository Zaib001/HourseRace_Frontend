import RegisterForm from "../components/RegisterForm";

const Register = ({ user }) => {
    return (
        <div>

            <RegisterForm user={user} />

        </div>
    );
};

export default Register;
